import { Meta } from "@storybook/blocks";

<Meta title="next/ShapeCache" />

The next shape cache functions slightly differently. Adds support for rehydrating paths on the client side.

The implementation matches [/]