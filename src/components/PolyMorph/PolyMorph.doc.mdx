import { Meta } from '@storybook/blocks';

<Meta title="src/components/PolyMorph/PolyMorph" />

## PolyMorph
`src/components/PolyMorph/PolyMorph`

**AST Node Type:** *VariableStatement*

#### JSDoc annotations:

PolyMorph is a simple tool to override the tagname and potentially use a custom component

```bash
@param param0

@returns
```

#### Code:

```ts
const PolyMorph = <T extends ElementType = "div">({ as, ...props }: PolyMorphProps<T>) => {
    const El = as || "div";
    return <El>{ ...props } /  >
    ;
};

```

