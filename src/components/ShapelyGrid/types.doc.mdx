import { Meta } from '@storybook/blocks';

<Meta title="src/components/ShapelyGrid/types" />

## ShapelyGridProps
`src/components/ShapelyGrid/types`

**AST Node Type:** *TypeAliasDeclaration*

#### JSDoc annotations:

The Shapely Grid props are used to layout the items so they can be displayed appropriately

```bash

```

#### Code:

```ts
export type ShapelyGridProps = {
    aspectRatio?: string;
    columns: number;
    layout?: LayoutFn;
    cellSize: [
        number,
        number
    ];
    columnCell?: string;
    columnPrefix?: string;
    columnSuffix?: string;
    clipPath?: string;
};

```

