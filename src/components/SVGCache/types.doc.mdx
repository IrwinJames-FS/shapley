import { Meta } from '@storybook/blocks';

<Meta title="src/components/SVGCache/types" />

## CachePreviewProps
`src/components/SVGCache/types`

**AST Node Type:** *TypeAliasDeclaration*

#### JSDoc annotations:

These properties are not part of the component

```bash

```

#### Code:

```ts
export type CachePreviewProps = {
    count: number;
    speeds: PreviewRotations;
};

```

