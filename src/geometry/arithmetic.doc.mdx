import { Meta } from '@storybook/blocks';

<Meta title="src/geometry/arithmetic" />

## round
`src/geometry/arithmetic`

**AST Node Type:** *VariableStatement*

#### JSDoc annotations:

I dont like the limit decimal method this forces conversion to string rather then just dropping precision

```bash
@param num

@param limit
```

#### Code:

```ts
export const round = (num: number, limit: number) => {
    if (!limit)
        return Math.round(num);
    const l = 10 ** limit;
    return Math.round(Math.round(num * l)) / l;
};

```

## toDeg
`src/geometry/arithmetic`

**AST Node Type:** *VariableStatement*

#### JSDoc annotations:

Convert radians to degress

```bash
@param rad

@returns
```

#### Code:

```ts
export const toDeg = (rad: number): number => rad * 180 / Math.PI;

```

## toRad
`src/geometry/arithmetic`

**AST Node Type:** *VariableStatement*

#### JSDoc annotations:

Convert degrees to radians

```bash
@param deg

@returns
```

#### Code:

```ts
export const toRad = (deg: number): number => deg * Math.PI / 180;

```

