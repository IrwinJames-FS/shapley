export const CIRCLE = Math.PI*2;

/*
I am pretty sure char codes are pretty standard but why risk it this is just a collection of char codes which will be used
*/
export const MIN_NUMERIC_CHAR_CODE = "0".charCodeAt(0);

export const MAX_NUMERIC_CHAR_CODE = "9".charCodeAt(0);

export const PERIOD_CHAR_CODE = ".".charCodeAt(0);